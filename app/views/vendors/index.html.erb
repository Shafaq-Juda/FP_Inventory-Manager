
<h1>Vendors</h1>

<%= link_to "New vendor", new_vendor_path %>

<div id="vendors">
  <% @vendors.each do |vendor| %>
    
     <table>
      <tr>
        <th>Name</th>
  
        <th>Details</th>
      </tr>
      <% @vendors.each do |vendor| %>
        <tr>
          <td> <%= vendor.name %></td>
         
          <td> <%= link_to "Show this vendor", vendor %></td>
          </tr>
 <% end %>
      </table>

  <% end %>
</div>
